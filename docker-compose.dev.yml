version: "3"
services:
  report-dev:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: "report_dev"
    ports:
      - "8082:80"
    networks:
      - report-dev
    restart: always
networks:
  report-dev:
    name: report-dev
    driver: bridge